# The deployed frontend's origin, for CORS. Leave unset locally (defaults to
# "*", which allows any origin -- fine for local dev, not for production).
# In production on Render, set this to your Vercel domain, e.g.:
# FRONTEND_URL=https://soil-health-monitoring.vercel.app
FRONTEND_URL=
